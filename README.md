# CrudApp
This CRUD (Create, Read, Update, Delete) application is a modern web-based project developed using React.js as the frontend framework and integrated with a RESTful API for data management. The application demonstrates the core principles of frontend development.
